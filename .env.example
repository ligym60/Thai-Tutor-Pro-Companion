# Example environment variables for local development
# Place a real `.env` in the project root (same folder as package.json)

# Optional: PostgreSQL connection (only required if you run DB migrations or use the DB-backed storage)
DATABASE_URL=postgresql://username:password@localhost:5432/sawasdeelearn

# Session secret for authentication (change to a strong secret)
SESSION_SECRET=change-me

# Host:port used by the client to call the API. Client code expects this to be set.
# For local dev where server runs on localhost:5000 use:
EXPO_PUBLIC_DOMAIN=localhost:5000

# Server port (defaults to 5000 if unset)
PORT=5000

# Optional Replit/build-related variables (used by scripts/build.js and CORS)
REPLIT_DEV_DOMAIN=
REPLIT_DOMAINS=
REPLIT_INTERNAL_APP_DOMAIN=https://replit.com/@ligym60/Thai-Tutor-Pro
